/**
 * Local re-export of @kitstack/sdk/view.
 * View components import from here.
 * When the real SDK ships, replace this with: export * from "../sdk-view"
 */
export { useKit, useTool, useFile } from "../../../packages/sdk/views/src/shared/use-kit";
