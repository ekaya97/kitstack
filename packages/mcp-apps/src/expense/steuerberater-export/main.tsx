import { createRoot } from "react-dom/client";
import "@shared/styles.css";
import { SteuerberaterExport } from "./SteuerberaterExport";

createRoot(document.getElementById("root")!).render(<SteuerberaterExport />);
